# Copy to .env for LOCAL EMULATOR TESTING ONLY.
# Never commit a real .env file — add it to .gitignore.
# For deployed functions, use Firebase Secrets instead (see index.js header).

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-sending-address@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM_NAME=Route2Uni
